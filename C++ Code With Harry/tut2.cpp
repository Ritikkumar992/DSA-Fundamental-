#include <iostream>  //-->Header files(improve the fuctionality of our program).
using namespace std; /*---> if u find something that is not declared 
                          in the current scope go and check std.*/
/*It is used because computer needs to know the  code for the cout or cin 
functionalites and it to which name space they are defined.*/

int main()//main-->>entry  point (function)
{
  cout << "Hello Ritik,How are you?";//(<<)--> insertion opeator/put to operator .
  return 0;
}
//cout is not a function but it is an output instruction, to send message to monitor.